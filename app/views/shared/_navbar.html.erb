<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom-navbar">
  <ul class="navbar-nav mx-auto">
    <li class="nav-item">
      <%= link_to  "MyOwnSpace", root_path, class: 'navbar-brand disabled brand-color' %>
    </li>
  </ul>
  <ul class="navbar-nav mx-auto">
    <li class="nav-item">
      <% if(current_page?(notes_path))%>
        <form class="form-inline">
          <div class="input-group">
            <div class="input-group-prepend">
              <span class="input-group-text" id="basic-addon1"><a href='<%= root_path %>'><%= image_tag 'home.png', width: 18, height: 18 %></a></span>
            </div>
            <input type="text" class="form-control" placeholder="Serach a Note" aria-label="Username" aria-describedby="basic-addon1">
          </div>
        </form>
      <% elsif (current_page?(new_note_path))%>
        <%= link_to 'Back to your notes', notes_path, class: 'btn btn-outline-warning' %>
      <% end %>
    </li>
  </ul>
  <% if user_signed_in? %>
  <ul class="navbar-nav dropdown mx-auto">
    <li class="nav-item">
      <a class="nav-link btn btn-sm btn-outline-secondary dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> Account </a>
      <div class="dropdown-menu border-dropdown">
          <p class="dropdown-item">Welcome, <%= current_user.username %>!</p>
          <hr class="dropdown-separator">
          <%= link_to 'My Profile', user_path(current_user), class: 'dropdown-item' %>
          <%= link_to 'Friends', root_path, class: 'dropdown-item' %>
          <%= link_to 'Configuraion', root_path, class: 'dropdown-item' %>
          <hr class="dropdown-separator">
          <%= link_to 'Log-out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
      </div>
    </li>
  </ul>
  <% else %>
  <ul class="navbar-nav mx-auto">
    <%= link_to 'Log-in', new_user_session_path, class: 'nav-item btn btn-sm btn-outline-secondary' %>
    <%= link_to 'Register', new_user_registration_path, class: 'nav-item btn btn-sm btn-outline-secondary' %>
  </ul>
  <% end %>
</nav>
